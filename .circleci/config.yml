# This config was automatically generated from your source code
# Stacks detected: cicd:jenkins:backend,deps:java:backend,deps:node:frontend,tool:gradle:
version: 2.1

jobs:
  gradle-build:
    docker:
      - image: cimg/openjdk:17.0
    working_directory: ~/project
    steps:
      - checkout
      - run:
          name: Add Permission
          command: chmod +x ./backend/gradlew
      - run:
          name: Gradle Build
          command: cd backend
      - run:
          name: Gradle Build
          command: ./gradlew bootJar
      - persist_to_workspace:
          root: .
          paths:
            - backend/build/libs/*.jar
workflows:
  version: 2
  build-and-deploy:
    jobs:
      - gradle-build